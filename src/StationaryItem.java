public class StationaryItem extends Product {
    StationaryItem(String title, String description, String id, String primaryImage, String[] secondaryImages, String brand,
                   Category category, String hsn, double mrp) {
        super(title, description, id, primaryImage, secondaryImages, brand, category, hsn, mrp);
    }
}
